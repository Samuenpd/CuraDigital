# CI/CD — GitHub Actions

Pasta destinada ao pipeline de CI/CD (ex.: ci.yml), responsavel por
rodar os testes (Jest, Cypress, testes de backend) automaticamente a
cada push/PR, antes de permitir o deploy. Ainda nao configurado.
