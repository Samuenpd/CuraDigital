PORT=3000
JWT_SECRET=troque-por-um-segredo-forte
CORS_ORIGIN=http://localhost:5173
DB_PATH=./database.sqlite

# Redis usado pela fila BullMQ (producer) — ver src/config/queue.js.
# Suba com `docker compose up -d` a partir da raiz do repositório.
REDIS_URL=redis://localhost:6379
